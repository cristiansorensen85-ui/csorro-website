const greetingEl=document.getElementById('greeting');const coreTitle=document.getElementById('coreTitle');const modal=document.getElementById('coreModal');function updateGreeting(){const h=new Date().getHours();let g='Good Morning',s='Morning';if(h>=12&&h<18){g='Good Afternoon';s='Afternoon'}if(h>=18){g='Good Evening';s='Evening'}greetingEl.textContent=`${g}, Cristian.`;coreTitle.textContent=`${s} Cristian. I’ve organised today’s work.`}function loadWorkspace(){const saved=localStorage.getItem('csorroCurrentWorkspace');if(!saved)return;try{const w=JSON.parse(saved);const grid=document.getElementById('workspaceGrid');const card=document.createElement('article');card.className='workspace-card';card.innerHTML=`<span>${w.type||'Workspace'}</span><h3>${w.name}</h3><p>${(w.priorities||[]).slice(0,3).join(', ')||'Ready to organise.'}</p><div class="progress"><i style="width:${w.progress||18}%"></i></div>`;grid.prepend(card);const item=document.createElement('li');item.textContent=`${w.name} workspace is ready and saved to Mission Control.`;document.getElementById('coreList').prepend(item);document.getElementById('projectsCount').textContent='4';document.getElementById('tasksCount').textContent='2'}catch(e){}}updateGreeting();loadWorkspace();document.getElementById('openBriefing').addEventListener('click',()=>modal.classList.remove('hidden'));document.getElementById('closeModal').addEventListener('click',()=>modal.classList.add('hidden'));document.getElementById('closeModal2').addEventListener('click',()=>modal.classList.add('hidden'));modal.addEventListener('click',e=>{if(e.target===modal)modal.classList.add('hidden')});document.getElementById('commandInput').addEventListener('keydown',e=>{if(e.key==='Enter')modal.classList.remove('hidden')});
